# Streamlit-in-Snowflake conda environment (packages from the Snowflake Anaconda channel).
name: sf_env
channels:
  - snowflake
dependencies:
  - streamlit
  - altair
  - pandas
  - snowflake-snowpark-python
