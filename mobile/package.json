{
  "name": "vahdam-lifecycle-os-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that ships VAHDAM Lifecycle OS to the Google Play Store and Apple App Store. Loads the hosted PWA (server.url), so web updates ship without resubmitting.",
  "scripts": {
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "run:android": "cap run android",
    "run:ios": "cap run ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
